version: "3.9"
volumes:
  postgres_data:

services:
  db:
    image: postgres
<<<<<<< HEAD
    volumes:
      - postgres_data:/var/lib/postgresql/data
||||||| parent of e26d456 (Add article scaffold with debugger in articles#index)
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
=======
>>>>>>> e26d456 (Add article scaffold with debugger in articles#index)
    environment:
      POSTGRES_PASSWORD: password
  web:
    tty: true
    build: .
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rdbg --open --nonstop -c -- rails s -p 3000 -b '0.0.0.0'"
    volumes:
      - .:/myapp
    ports:
      - "3000:3000"
    depends_on:
      - db
